apiVersion: v1
kind: Service
metadata:
  name: {{ include "ldap-sync.fullname" . }}
  labels:
    {{- include "ldap-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ldap-sync.selectorLabels" . | nindent 4 }}
