apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ldap-sync.fullname" . }}-test-connection"
  labels:
    {{- include "ldap-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ldap-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
